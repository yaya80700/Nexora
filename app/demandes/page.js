"use client";
import {useEffect,useState} from "react"; import Link from "next/link"; import {ArrowLeft,LoaderCircle,MessageCircle,Send} from "lucide-react"; import Header from "../ui/Header"; import Footer from "../ui/Footer";
const status={new:"Nouvelle",in_progress:"En cours",answered:"Réponse Nexora",closed:"Clôturée"};
export default function Demandes(){const [requests,setRequests]=useState([]),[messages,setMessages]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[text,setText]=useState({});async function load(){const r=await fetch("/api/demandes");const j=await r.json();if(!r.ok)setError(j.error||"Erreur");else{setRequests(j.requests||[]);setMessages(j.messages||[])}setLoading(false)}useEffect(()=>{load()},[]);async function reply(id){if(!text[id]?.trim())return;const r=await fetch("/api/demandes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({request_id:id,message:text[id]})});const j=await r.json();if(!r.ok)setError(j.error||"Erreur");else{setText(x=>({...x,[id]:""}));load()}}return <main><Header/><section className="subPage"><div className="subHero"><Link href="/compte" className="editorBack"><ArrowLeft size={15}/> Mon espace</Link><span className="sectionTag">MES DEMANDES</span><h1>Vos échanges<br/><span>avec Nexora.</span></h1><p className="lead">Retrouvez vos demandes, les réponses de Nexora et poursuivez la conversation directement ici.</p>{loading?<div className="adminEmpty"><LoaderCircle className="spin"/> Chargement…</div>:error?<p className="authError">{error}</p>:requests.length===0?<div className="emptyState"><MessageCircle size={30}/><h2>Aucune demande</h2><p>Vous n'avez pas encore envoyé de demande.</p><Link href="/contact" className="primary">Créer une demande <Send size={15}/></Link></div>:<div className="requestList">{requests.map(r=><article className="requestCard" key={r.id}><div className="requestHead"><div><span className="sectionTag">{r.request_type}</span><h2>{r.subject}</h2></div><span className={`requestStatus ${r.status}`}>{status[r.status]||r.status}</span></div><p className="requestOriginal">{r.message}</p><div className="requestMessages">{messages.filter(m=>m.request_id===r.id).map(m=><div className={`requestMessage ${m.sender_role}`} key={m.id}><b>{m.sender_role==="admin"?"Nexora":"Vous"}</b><p>{m.message}</p><small>{new Date(m.created_at).toLocaleString("fr-FR")}</small></div>)}</div><div className="requestReply"><textarea rows="3" placeholder="Votre réponse…" value={text[r.id]||""} onChange={e=>setText(x=>({...x,[r.id]:e.target.value}))}/><button className="primary" onClick={()=>reply(r.id)}>Répondre <Send size={15}/></button></div></article>)}</div>}</div></section><Footer/></main>}
