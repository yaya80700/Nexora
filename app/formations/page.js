import Link from "next/link";import {ArrowRight,CheckCircle2,Clock3,GraduationCap,Sparkles} from "lucide-react";import Header from "../ui/Header";import Footer from "../ui/Footer";import {getCatalog} from "../../lib/nexora/catalog";import {getSiteContent} from "../../lib/nexora/siteContent";import ContentBlocks from "../ui/ContentBlocks";
export default async function Formations(){const [formations,c]=await Promise.all([getCatalog("formation"),getSiteContent("formations")]);return <main><Header/><section className="pageHero"><span className="sectionTag">{c.eyebrow}</span><h1>{c.title}<br/><span>{c.accent}</span></h1><p>{c.text}</p></section><section className="filterRow"><span><GraduationCap size={17}/> {formations.length} formations disponibles</span><span><Sparkles size={15}/> Contenu pratique · progression libre</span></section>{c.showCatalog!==false&&<section className="formationGrid">{formations.map(f=><article className="formationCard" key={f.slug}><div className="formationIcon">{f.icon}</div><div className="cardMeta">{f.category}<span>{f.level}</span></div><h2>{f.title}</h2><p>{f.description||f.desc}</p><div className="formationInfo"><span><Clock3 size={14}/> Parcours progressif</span><span><CheckCircle2 size={14}/> Projet pratique</span></div><ul>{(f.bullets||[]).slice(0,3).map(x=><li key={x}><CheckCircle2 size={15}/>{x}</li>)}</ul><div className="priceRow"><strong>{f.price!=null?`${Number(f.price).toFixed(2).replace(".",",")} €`:f.price_label||"Sur devis"}</strong><Link className="smallBtn" href={`/formations/${f.slug}`}>Voir le programme <ArrowRight size={16}/></Link></div></article>)}</section>}{c.showCta!==false&&<section className="serviceCTA formationCTA"><div><span className="sectionTag">BESOIN D'UNE FORMATION DIFFÉRENTE ?</span><h2>{c.ctaTitle}</h2><p>{c.ctaText}</p></div><Link className="primary" href="/contact">Demander une formation <ArrowRight size={17}/></Link></section>}<ContentBlocks blocks={c.customBlocks}/><Footer/></main>}
