"use client";
import {useEffect,useState} from "react"; import Link from "next/link"; import {createClient} from "../../../lib/supabase/client"; import {LoaderCircle,Send,MessageCircle,LogOut} from "lucide-react";
import AdminControlNav from "../AdminControlNav";
const labels={new:"Nouvelle",in_progress:"En cours",answered:"Répondue",closed:"Clôturée"};
export default function AdminDemandes(){const [requests,setRequests]=useState([]),[messages,setMessages]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[text,setText]=useState({});async function load(){const r=await fetch("/api/admin/demandes");const j=await r.json();if(!r.ok)setError(j.error||"Erreur");else{setRequests(j.requests||[]);setMessages(j.messages||[])}setLoading(false)}useEffect(()=>{load()},[]);async function reply(id){if(!text[id]?.trim())return;const r=await fetch("/api/admin/demandes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({request_id:id,message:text[id]})});const j=await r.json();if(!r.ok)setError(j.error||"Erreur");else{setText(x=>({...x,[id]:""}));load()}}async function status(id,status){await fetch("/api/admin/demandes",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status})});load()}
return <main className="adminPage"><header className="adminTop"><div><span className="sectionTag">NEXORA ADMIN</span><h1>Les demandes <span>Nexora.</span></h1><p>Consultez les demandes et répondez directement depuis votre compte administrateur.</p></div><div className="adminActions"><Link href="/" className="secondary">Voir le site</Link><button className="secondary" onClick={async()=>{await createClient().auth.signOut();location.href="/admin/connexion"}}><LogOut size={15}/> Déconnexion</button></div></header><section className="adminWrap"><AdminControlNav active="requests" />{error&&<p className="authError">{error}</p>}{loading?<div className="adminEmpty"><LoaderCircle className="spin"/> Chargement…</div>:requests.length===0?<div className="adminEmpty"><MessageCircle size={30}/> Aucune demande pour le moment.</div>:<div className="requestList adminRequests">{requests.map(r=><article className="requestCard" key={r.id}><div className="requestHead"><div><span className="sectionTag">{r.request_type} · {r.email}</span><h2>{r.subject}</h2><p className="requestMeta">{r.name} · {new Date(r.created_at).toLocaleString("fr-FR")}</p></div><select value={r.status} onChange={e=>status(r.id,e.target.value)}><option value="new">Nouvelle</option><option value="in_progress">En cours</option><option value="answered">Répondue</option><option value="closed">Clôturée</option></select></div><p className="requestOriginal">{r.message}</p><div className="requestMessages">{messages.filter(m=>m.request_id===r.id).map(m=><div className={`requestMessage ${m.sender_role}`} key={m.id}><b>{m.sender_role==="admin"?"Nexora":"Client"}</b><p>{m.message}</p><small>{new Date(m.created_at).toLocaleString("fr-FR")}</small></div>)}</div><div className="requestReply"><textarea rows="3" placeholder="Répondre au client…" value={text[r.id]||""} onChange={e=>setText(x=>({...x,[r.id]:e.target.value}))}/><button className="primary" onClick={()=>reply(r.id)}>Envoyer la réponse <Send size={15}/></button></div></article>)}</div>}</section></main>}
